<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.danusuhendra.suitgamev3">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".ui.multiplayer.view.MultiPlayerActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ui.singleplayer.view.SinglePlayerActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ui.editprofile.EditProfileActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ui.savegame.view.SaveBattleActivity" android:theme="@style/AppTheme"/>
        <activity android:name=".ui.register.view.RegisterActivity" />
        <activity android:name=".ui.login.view.LoginActivity" />
        <activity android:name=".ui.landingpage.LandingPageActivity" />
        <activity android:name=".ui.splashscreen.view.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity">

        </activity>
    </application>

</manifest>